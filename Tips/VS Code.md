`@'symbol name'` in command palette to find the symbol in the file
`#'symbol name'` in command palette to find the symbol in the project

`CTRL + G:` Go to specific line within the code
`CMD + D:` Highlight the selected word multiple times and allow multi-line edit
  * Similar to `Option + Click`

`CMD + L:` Highlight the whole line